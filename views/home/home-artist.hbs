{{!-- <div class="container my-5">
  <div class="row">
    
      <h1>Open Candidatures</h1>
      <div class="home-artist-3">

         {{#each jobs as |job|}}
          {{> jobcard job=job currentUser=../currentUser}}
          {{!-- <form method="POST" action="/jobs/{{job.id}}/application">
            <button type="button" class="btn btn-primary">Apply</button>
            <button class="link">Apply</button> {{! helper para que cambie a "applied"? }}
          </form> --}}
        {{!-- {{/each}}
     
    </div>
  </div>
</div> --}} 


<div>
  <section class="border-bottom-0 border-top-0 w-100 p-4">
      <h1>Be casted in...</h1>

    <div class="container">
      <h3>Open positions ({{jobs.length}})</h3>
        <div class="row">
          {{#each jobs as |job|}}
              <div class="col">
                
                  {{> jobcard job=job currentUser=../currentUser}}
                  
              </div>
          {{/each}}  
        </div>
    </div>
  </section>
    
</div>    


    {{!--   <div class="row">
        {{#each jobs as |job|}}
          <div class="col-md-3">
            <div class="d-flex gap-3">
              {{> jobcard job=job currentUser=../currentUser}}
            </div>
          </div>
        {{/each}}
      </div>
  </section>
</div> --}}