
<div> 
  <h1 class="d-flex gap-3 align-center">You're looking for...</h1>
    <h3>Your posted jobs ({{jobs.length}})</h3>

{{!-- <div class="card text-center">
  {{#each jobs as |job|}}
      {{#isOwner currentUser=../currentUser jobOwnerId=job.owner._id}}
          <div class="card-header">
                  <ul class="nav nav-tabs card-header-tabs" role="tablist">
                  {{#each jobs as |job|}}
                    <li class="nav-item">
                      <button class="nav-link active" id="home-tab" data-bs-toggle="tab" data-bs-target="#home-tab-pane" type="button" role="tab" aria-controls="home-tab-pane" aria-selected="true">{{job.title}}</button>
                    </li>
                  {{/each}}
                  </ul>
            </div>
          <div class="card-body">
            {{> jobposted job=job currentUser=../currentUser}}
          </div>
      {{/isOwner}}
      {{else}}
            <div>
              <h1>You have no jobs posted yet</h1>
            </div>
{{/each}}
</div>

<form class="text-end" style="margin-top:100px"> {{! poner el botón a la derecha abajo}}
          <a class="btn " href="/create" method="GET" action="/create">
            Create new job
          </a>
      </form>
    </div>
  </div>
</div>
 --}}









 <div class="d-flex gap-3 align-center">
    <div class="col align-center">
      <section class="border-bottom-0 border-top-0 w-100 p-4">
          {{#each jobs as |job|}}
            {{#isOwner currentUser=../currentUser jobOwnerId=job.owner._id}}
                  {{> jobposted job=job currentUser=../currentUser}}
          
              {{/isOwner}}
          {{else}}
            <div>
              <h1>You have no jobs posted yet</h1>
            </div>
          {{/each}}
      </section>

      <form class="text-end" style="margin-top:20px; margin-bottom:50px"> {{! poner el botón a la derecha abajo}}
          <a class="btn btn-primary" href="/create" method="GET" action="/create">
            Create new job
          </a>
      </form>
    </div>
  </div>
</div>
 
 

